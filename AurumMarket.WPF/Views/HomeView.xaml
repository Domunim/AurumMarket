<UserControl x:Class="AurumMarket.WPF.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AurumMarket.WPF.Views"
             
             xmlns:controls="clr-namespace:AurumMarket.WPF.Controls"
             
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Home Screen"/>
                    <!-- TODO -    CONTINUE HERE!!    index to asset? asset bound to card -->
        <controls:MetalIndexCard Grid.Row="1" Margin="0 10" HorizontalAlignment="Left" DataContext="{Binding MetalIndexViewModel.Gold}"/>

    </Grid>
</UserControl>
